# Test Standalone Web Forms

Small project to test standalone Web Forms

- Drop forms in the public folder
- Import the forms in the App component
- Add the link in the template

Setup and run:

```bash
yarn install

yarn build 

yarn dev
```
